[project]
name = "runner"
version = "0.1.0"
description = "Simulation core runner for PISA"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pisa-api",
]

[tool.uv.sources]
pisa-api = { git = "https://github.com/PISA-Hut/pisa-api.git" }
